audit:
  ignore:
    - STR-016    # README alongside SKILL.md is intentional
  # Whitelist documentation/blog domains for cost optimization references (won't trigger SEC-002)
  safe_domains:
    - aws.amazon.com
    - docs.aws.amazon.com
    - repost.aws
